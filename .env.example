# agent-trace CLI configuration
# Copy this to .env and edit as needed.
# After install, this file lives at ~/.agent-trace/.env

# Service URL (default: http://localhost:5000)
AGENT_TRACE_URL=http://localhost:5000
